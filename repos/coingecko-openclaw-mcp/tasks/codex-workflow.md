# Codex Workflow

1. Write or update tests first for new behavior.
2. Keep MCP tools read-only and deterministic where possible.
3. Prefer structured outputs that agents can recombine.
4. Verify with `npm run build` and `npm test`.
