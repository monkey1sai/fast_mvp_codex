[mcp_servers.pulseIngestor]
command = "docker"
args = ["compose", "run", "--rm", "-T", "mcp", "python", "-m", "app.mcp_server"]
cwd = "."
enabled = true
required = false
startup_timeout_sec = 30
tool_timeout_sec = 60

